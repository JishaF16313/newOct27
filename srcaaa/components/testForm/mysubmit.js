
export const submit = (values, dispatch, props) => {
    console.log(props,"PROPSSSSSSSSSSSSSSS") // Logs the props from redux-form, as well as the props available to the React component which contains the form.
  }